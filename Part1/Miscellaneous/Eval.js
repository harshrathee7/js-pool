// Eval: run a code string

/*The built-in eval function 
allows to execute a string of code.*/

// the syntax:let result = eval(code)

let code = 'console.log("Hello")';

eval(code);

let sum = "1+2";

console.log(eval(sum));
